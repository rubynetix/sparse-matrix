language: ruby
sudo: false
dist: trusty

rvm: 2.5.3

stages:
  - build
  - identities
  - sparse_tests

jobs:
  include:
    - stage: build
      script:
        - ./scripts/setup.sh
    - stage: sparse_tests
      script:
        - ruby test/sparse_matrix_test.rb
    - stage: tridiagonal_tests
      script:
        - ruby test/tridiagonal_matrix_test.rb
